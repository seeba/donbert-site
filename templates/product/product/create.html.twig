{% extends 'admin_base.html.twig' %}

{% block content %}
    <div class="row">
    <div class="col-12 col-xl-6">
        <div class="card">
            <div class="card-header">

            </div>
            <div class="card-body">
                {{ form_start(form) }}
                {{ form_end(form) }}
            </div>

        </div>
    </div>
</div>
    {{ form_start(form) }}
    {{ form_end(form) }}
{% endblock %}